name: Build Android APK

on:
  push:
    branches: [main]
    tags: ["*"]
    paths:
      - "android/**"
      - "packages/plugin/**"
      - "packages/protocol/**"
      - "packages/server/**"
      - "packages/sn-stub/**"
      - "package.json"
      - "package-lock.json"
      - "scripts/embed-onboard-page.mjs"
      - ".github/workflows/build-android-apk.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build companion APK and Linux server
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"
      - run: npm run build:server
      - run: npm run build:server:arm64
      - name: Name the distributable servers
        run: |
          cp dist/olaink-server ola-ink-server-linux-x64
          cp dist/olaink-server-linux-arm64 ola-ink-server-linux-arm64
      - uses: actions/upload-artifact@v4
        with:
          name: ola-ink-server-linux-x64
          path: ola-ink-server-linux-x64
          if-no-files-found: error
          retention-days: 14
      - uses: actions/upload-artifact@v4
        with:
          name: ola-ink-server-linux-arm64
          path: ola-ink-server-linux-arm64
          if-no-files-found: error
          retention-days: 14
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle
      - run: ./gradlew :app:assembleDebug
        working-directory: android
      - name: Name the distributable APK
        run: cp android/app/build/outputs/apk/debug/app-debug.apk ola-ink-companion.apk
      - uses: actions/upload-artifact@v4
        with:
          name: ola-ink-companion-debug-apk
          path: ola-ink-companion.apk
          if-no-files-found: error
          retention-days: 14
      - name: Publish GitHub release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" ola-ink-companion.apk ola-ink-server-linux-x64 ola-ink-server-linux-arm64 --repo "$GITHUB_REPOSITORY" --title "Ola Ink $GITHUB_REF_NAME" --generate-notes
